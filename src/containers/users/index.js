import React, { memo } from 'react';
import {
    MainContainer,
    Card,
} from './styles';

function Users() {

    return (
        <MainContainer>
            <Card>
                una routa
            </Card>
        </MainContainer>
    );
}

export default memo(Users);
